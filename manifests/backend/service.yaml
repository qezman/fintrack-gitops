apiVersion: v1
kind: Service
metadata:
  name: fintrack-backend
  namespace: fintrack
spec:
  type: ClusterIP
  selector:
    app: fintrack-backend
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP